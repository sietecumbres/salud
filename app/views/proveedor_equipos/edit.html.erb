<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to "#{t("web-app-theme.list", :default => "Lista")}", proveedor_equipos_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "Nuevo")}", new_proveedor_equipo_path %></li>
    </ul>
  </div>          
  <div class="content">            
    <h2 class="title">Editar Proveedor de Equipo</h2>
    <div class="inner">
      <%= form_for @proveedor_equipo, :url => proveedor_equipo_path(@proveedor_equipo), :html => { :class => :form } do |f| -%>
        <%= render :partial => "form", :locals => {:f => f} %>
      <% end -%>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'layouts/sidebar') -%>