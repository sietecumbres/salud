<tr>
  <td><%= equipo.modelo %></td>
  <td><%= equipo.marca %></td>
  <td><%= equipo.serial %></td>
  <td><%= equipo.placa %></td>
  <td><%= link_to 'Detalles', equipo_path(equipo) %></td>
  <td><%= link_to 'Editar', edit_equipo_path(equipo) %></td>
  <td><%= link_to 'Eliminar', equipo_path(equipo), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Esta seguro ?")}" %></td>
</tr>